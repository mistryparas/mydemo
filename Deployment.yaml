apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloworld
  namespace: duclo-dev
spec:
  selector:
    matchLabels:
      app: hello-duclo
  replicas: 4
  strategy:
    type: Recreate

  template:
    metadata:
      labels:
        app: hello-duclo
    spec:
      containers:
      - name: helloworld-dev
        image: 192.168.56.250:8083/docker-helloworld:1.13
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          protocol: TCP
      imagePullSecrets:
        - name: nexusrepo-dev
